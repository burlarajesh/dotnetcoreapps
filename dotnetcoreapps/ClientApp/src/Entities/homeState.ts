import { homePageEntity } from "./homePage";

export class homePageState {
    isLoaded: boolean;
    data: homePageEntity;
}

export class stateStore {
    id: number;
    isLoaded: boolean;
    data: homePageEntity;
}